function kauth() {
  local u=$(kubectl config get-users | grep $1)
  kubectl config set-credentials $u --token $2
}
eval "$(oh-my-posh --init --shell zsh --config ~/git/dotfiles/poshtheme_kube.omp.json)"
